import styled from "@emotion/styled";
import { Box } from "@mui/material";
export const StyledErorrBox = styled(Box)({
  display: "flex",
  justifyContent: "center",
  alignContent: "center",
  alignItems: "center",
  justifyItems: "center",
  height: "100vh",
});

export const StyledSpanBox = styled(Box)({
  padding: "12px 8px",
  fontSize: "1.3rem",
});
